import React from 'react'

/* Component to display animated spinner when loading */
export function LoadingSpinner() {
    return (
        <div className="spinner-border text-primary" role="status"></div>
    )
}
